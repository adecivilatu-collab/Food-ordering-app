# Customer PWA (Next.js, free tier)
Location → Restaurant → Food → Cart → Payment → Delivery → Review → Reorder.
Guest browse + guest checkout. OSM/Leaflet map, test-mode payments, console OTP in dev.
