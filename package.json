{
  "name": "food-ordering-app",
  "version": "0.1.0",
  "private": true,
  "description": "Nigerian multi-restaurant food marketplace — free-tier monorepo",
  "workspaces": [
    "apps/*",
    "packages/*",
    "api"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace apps/web-customer",
    "dev:api": "npm run dev --workspace api",
    "build": "npm run build --workspaces --if-present",
    "lint": "echo \"lint placeholder — add eslint\"",
    "test": "echo \"test placeholder\""
  }
}
